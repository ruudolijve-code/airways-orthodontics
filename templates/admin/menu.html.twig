{% extends '@EasyAdmin/menu.html.twig' %}

{% block main_menu %}

    <div class="ea-sidebar-header border-b border-slate-200 px-5 py-5">

        <div class="text-xs font-semibold uppercase tracking-[0.18em] text-teal-700">
            Airways Orthodontics
        </div>

        <div class="mt-2 text-lg font-semibold text-slate-900">
            Beheeromgeving
        </div>

    </div>

    {{ parent() }}

{% endblock %}